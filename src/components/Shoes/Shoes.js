import React from 'react';
import { add, multiply, substruct } from '../Utilities/Calculate';


const Shoes = () => {
  // const first = 30;
  // const second = 20;
  // const result = multiply(first, second)
  // const sum = add(first, second);
  // const substruc=substruct(first,second)
  return (
    <div>
      <h1>This is shoes</h1>
      {/* <p>Result: {result} and Total: {sum}</p>
      <p>substruct: {substruc}</p> */}
    </div>
  );
};

export default Shoes;